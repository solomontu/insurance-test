// Initialize a default theme variable..
// This gets reinitialized in the color service

import 'package:uitest/config/theme.dart';
import 'package:uitest/data/models/themes.dart';

ThemeModel colors = AppColors().defaultTheme;
